#!/usr/bin/env bash
set -euo pipefail

bun install
echo
echo "Frontend dependencies installed."
echo "Create a Python virtual environment and install apps/api dependencies before running the backend."
